# In your project folder, create README.md:
echo "# Real-time Satellite Location Tracker

A DBMS project that tracks the International Space Station (ISS) in real-time.

## Features
- Live ISS tracking with interactive map
- MySQL database for storing position history
- Admin panel with full CRUD operations
- CSV export functionality

## How to Run
1. Install dependencies: \`pip install -r requirements.txt\`
2. Configure MySQL credentials in \`app.py\`
3. Run: \`python app.py\`

## Technologies
- Backend: Python, Flask
- Database: MySQL
- Frontend: Leaflet.js, HTML, CSS, JavaScript
" > README.md

# Add, commit, and push the README
git add README.md
git commit -m "docs: add README with project details"
git push origin main# real-time-satellite-tracker
an DBMS mini project 
